/*citySuggestions.addEventListener('click', (event) => {
        if (!event.target.classList.contains('citySuggestion')) return;
        const id = event.target.id;
        for (let i = 0; i < suggestedCityInfo.result.length; i++) {
            const suggestedCitiesInfoElement = suggestedCityInfo[i];
            if (suggestedCityInfo.id === id) {
                const location = {
                    latitude: suggestedCityInfo.latitude,
                    longitude: suggestedCityInfo.longitude
                }
                console.log(location)
                // localStorage.setItem('location', )
                break;
            }
        }
    })*/